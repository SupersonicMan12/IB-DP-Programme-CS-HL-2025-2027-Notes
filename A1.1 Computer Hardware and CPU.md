## CPU Structure and Machine Cycle (1, 5, 6)

##### Parts of CPU (脑子)

A1.1.1 Describe the functions and interactions of the main CPU components. 
- Units: arithmetic logic unit (ALU), control unit (CU) 
- Registers: instruction register (IR), program counter (PC), memory address register (MAR), memory data register (MDR), accumulator (AC)
- Buses: address, data, control 
- Processors: single core processor, multi-core processor, co-processors 
- A diagrammatic representation of the relationship between the specified CPU components

A1.1.5 Describe the fetch, decode and execute cycle. 
- The basic operations a CPU performs to execute a single instruction in machine language 
- The interaction between memory and registers via the three buses: address, data, control 

A1.1.6 Describe the process of pipelining in multi-core architectures. (HL only) 
- The instructions fetch, decode, execute 
- Write-back stages to improve the overall system performance in multi-core architectures 
- Overview of how cores in multi-core processors work independently and in parallel 

Von Neumann Architecture: input, cpu, output.

**Units**: Arithmetic Logic Unit (ALU) and Control Unit (CU)
**Registers**:
- Instruction Register (IR) - current instruction
- Program Counter (PC) - address of next instruction
- Memory Address Register (MAR) - address of memory being accessed
- Memory Data Register (MDR) - data being read from or written to memory
- Accumulator (AC) - intermediate arithmetic/logic results
**Buses**:
- Address Bus - carries memory addresses
- Data Bus - carries data
- Control Bus - carries instructions 
**Processor**:
- Single-core processor - one instruction at a time
- Multi-core processor - parallel execution
- Co-processor - specialized secondary processor (graphics)

![[Screenshot 2026-01-14 at 21.22.36.png]]
##### Fetch–Decode–Execute Cycle 

Timed and synchronized by **clock**: a frequency for operations (in GHz)

**Fetch**:
- PC holds next address
- MAR receives the address from PC
- CU controls: 
	- address bus carries address from MAR
	- control bus carries signal to read
	- data bus returns data or instruction
- MDR holds the fetches data or instruction
- IR stores the instruction for decoding
**Decode**:
- CU reads instruction from IR
- Decodes via CPU instruction --> Opcode + Operand
**Execute**:
- ALU performs operation, returning to AC or memory

##### Pipelining

Instead of this:

| F1  | D1  | E1  |     |     |     |
| --- | --- | --- | --- | --- | --- |
|     |     |     | F2  | D2  | E2  |
|     |     |     |     |     |     |

Do this:

| F1  | D1  | E1  |     |     |
| --- | --- | --- | --- | --- |
|     | F2  | D2  | E2  |     |
|     |     | F3  | D3  | E3  |

This is within a **single core**. Across multiple cores, we call that **parallelism**.

Advantage: faster processing
## GPU and GPU vs CPU (2, 3)

A1.1.2 Describe the role of a GPU. 
- The architecture that allows graphics processing units (GPUs) to handle specific tasks and makes them suitable for complex computations 
- Real-world scenarios may include video games, artificial intelligence (AI), large simulations and other applications that require graphics rendering and machine learning. 
A1.1.3 Explain the differences between the CPU and the GPU. (HL only) 
- Differences in their **design philosophies**, usage scenarios 
- Differences in their core architecture, processing power, memory access, power efficiency 
- CPUs and GPUs working together: task division, data sharing, coordinating execution 

*Focus on differences in design philosophy*

Main advantage of GPU = **parallel processing**

It allows for real-world applications like video games, graphic rendering, AI, large simulations etc.

On the other hand, CPU = fewer cores, **but more powerful and higher clock speed**

| CPU                   | GPU                                                |
| --------------------- | -------------------------------------------------- |
| Sequential Processing | Massive Parallel Processing                        |
| Few Powerful Cores    | More Cores                                         |
| High Clock            | High-throughput parallel workloads                 |
| General Computers     | Repetitive-task-specific: AI training, video games |

## Types of Primary Memory (4)

A1.1.4 Explain the purposes of different types of primary memory. 
- Random-access memory (RAM), read only memory (ROM), cache (L1, L2, L3), registers 
- The interaction of the CPU with different types of memory to optimize performance 
- The relevance of the terms “cache miss” and “cache hit” 

CPU + Registers are not memory

From closest to farthest to CPU: L1 < L2 < L3 < Primary Memory(RAM/ROM) < Secondary Storage

L1 - L3 are Caches, or the "frequently accessed memory"
L1 to L3: faster in speed, smaller in size
RAM = random access memory
ROM = read only memory, for initialization etc.

SRAM = faster but more expensive than DRAM; Caches are SRAM, main RAM is DRAM

When a CPU needs something, first checks L1, then L2, then L3, then RAM

**Cache miss** = when the fetch's target is not in cache, meaning it looks in RAM, leading to longer fetch time and slower cycles

**Cache hit** = opposite, good


> [!NOTE] From S1M2:
> E.g. Effect of Cache Size? 
> Greater size smaller probability of **Cache Miss**, greater probability of **Cache Hit**, hence shorter fetch time and faster cycles

## Secondary Memory Storage (7)
A1.1.7 Describe internal and external types of secondary memory storage. 
- Internal hard drives: solid state drive (SSD), hard disk drive (HDD), embedded multimedia cards (eMMCs) 
- External hard drives: SSD, HDD, optical drives, flash drives, memory cards, network attached storage (NAS) 
- The scenarios in which the various types of drive are used 

Internal Hard Drives:
- Solid State Drive (SSD) - stability, speed and durability at high cost
- Hard Disk Drive (HDD) - large capacity at low cost
- Embedded multimedia cards (eMMCs)
External Hard Drives:
- SSD, HHD
- Optical Drives: CD/DVD/Blu-ray
- Flash Drives (USB)
- Memory Cards
- Network attached storage (NAS)

Mediums:
- Magnetic storage: HDD
- Optical storage: Optical Drives
- Solid-state / electronic storage: SSD, eMMC, flashdrives, memory cards

Sequential Access vs. Direct Access
## Compression (8)
A1.1.8 Describe the concept of compression. 
- The differences between lossy compression methods and lossless compression methods
- Run-length encoding and transform coding 

Lossy compression = from compressed, you cannot obtain the original file *exactly*!
Lossless compression = not lossy

Types of lossless compression:
- Run-length encoding: AAAAA = 5A 
- Transform coding: e.g. Huffman coding

## Cloud Computing (9)
A1.1.9 Describe the different types of services in cloud computing. 
- Services: software as a service (SaaS), platform as a service (PaaS), infrastructure as a service (IaaS) 
- The differences between the approaches of SaaS, PaaS, and IaaS in various real-world scenarios, recognizing that different degrees of control and flexibility influence resource management and resource availability

*Focus on flexibility, and varying degress of control over resources*

How bad are you at CS and other things?
- Very bad: Software as a service (SaaS), e.g. Google Docs
- Less bad: Platform as a service (PaaS), e.g. Google App Engine
- Pro: Infrastructure as a service (IaaS), e.g. Amazon EC2, directly database etc.



