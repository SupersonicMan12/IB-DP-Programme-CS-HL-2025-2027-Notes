### A2.3.1 Describe different types of IP addressing
- The distinction between IPv4 and IPv6 addressing
- The differences between public IP addresses and private IP addresses, and between static IP addresses and dynamic IP addresses 
- The role of network address translation (NAT) to minimize the use of IP addresses and to facilitate communication between private internal networks and the public internet

IPv4: 
![[Screenshot 2026-04-16 at 08.59.03.png]]
Prefixes: 0, 10, 110 (length l), then network prefix has length 8l, the rest is host.
Host cannot be: 000000 (network itself), 111111 (all devices)

Public IP Address:
- Managed by IANA, needs registration; no collisions allowed
- Connected to internet directly

Private IP Address:
- Computers not connected to the internet, need not have globally unique IP addresses, only through TCP/IP
- Use private IP address, network address translation (NAT) to translate to a public IP address that everyone in the LAN shares
- Private addresses look like:
	- A (8 prefix), B (12 prefix), C (16 prefix)
- Allows for duplicates globally

NAT - network address translation:
- Cause: not enough IPv4, IPv6 gives alternative, but is slow and costly to be adopted
- Solution: use a single or a few public addresses to encompass a bunch of private IP addresses
### A2.3.2 Compare types of media for data transmission. 
- Wired transmission via **fibre optic cables** and **twisted pair cables**; **wireless transmission** 
- The advantages and disadvantages of these three types of data transmission 
- Factors to consider must include bandwidth, complexity of installation, cost, range, susceptibility to: interference, attenuation, reliability, security.

Fibre optic cable: uses light pulses through glass / plastic fibers:
- high cost, very high bandwidth

Twisted pair cables: electrical signals through twisted copper wires:
- cheap, easy to install, lower bandwidth

Wireless transmission: via radio waves or microwaves, mobility, disadvantage:
- speed
- range
- licensing
### A2.3.3 Packet Switching
- The process of segmenting data into packets with a routing header attached, and independently transmitting control information, allowing the data to be reassembled at the destination 
- The role that switches and routers play in packet switching

Pay attention to the boldfaced keywords, these are what you should explain in answer.

Network communication method: ***segment*** the message into several, manageable "packets". Each ***packet*** contains a ton of data:
- ***header***: source, destination address, sequence number, error check-sum, lifetime of packet
- data: data itself

These packets are transmitted independently and reassembled. Each router pushes it on its best direction available, meaning the packets take different routes (***routing***); ***switches*** can forward packets on local area network by MAC address.

***Reassembly***: reassemble by sequence number and requests for resend for lost packets.

Benefits: make them up, it's faster, more efficient...

### A2.3.4 Static and Dynamic Routing
 - The process of static routing, and its advantages and disadvantages 
 - The process of dynamic routing, and its advantages and disadvantages (explanation of a specific routing protocol is not required) 
 - Factors to consider must include configuration, maintenance, complexity, resource usage, convergence, scalability, network size.

**Static Routing**: manually configure the routing table, the path is fixed for all packets:
- better for smaller networks
**Dynamic Routing**: automatic configuration of routing table:
- scalable
- less susceptible to transmission being affected by node breakdown


